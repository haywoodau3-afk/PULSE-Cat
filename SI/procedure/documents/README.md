# Procedure documents

This directory provides the procedure-document trees referenced by Section S9.6
of the final Supporting Information. The `si2`, `si3`, and `si4` entries point
to the corresponding procedure records in the repository's release packages.
The top-level `SI/procedure` directory also contains the manifests and the
cross-package audit documents used by the final release map.
